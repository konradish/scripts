secrets() {
	mkdir -p $HOME/secrets
	echo "No secrets to install"
	# If you have secrets hosted on another machine, put the command to copy them here
	# For example:
	# rsync -av user@host.lan:~/secrets ~/
}